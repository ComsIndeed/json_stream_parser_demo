name: json_stream_parser_demo
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_animate: ^4.5.2
  flutter_markdown_plus: ^1.0.5
  google_fonts: ^6.3.2
  provider: ^6.1.5+1
  llm_json_stream: ^0.1.0
  google_generative_ai: 0.4.5
  responsive_framework: ^1.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
