import 'package:flutter/material.dart';

class OtherDemosPage extends StatelessWidget {
  const OtherDemosPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Other Demos'),
      ),
      body: const Center(
        child: Text('Other Demos Page Content'),
      ),
    );
  }
}
